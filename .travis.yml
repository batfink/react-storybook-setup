language: node_js
node_js:
    - '10'
install:
    - npm ci
cache:
    directories:
        - "$HOME/.npm"
before_install:
    .travis/before_install.sh
    # - printf "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" >> ~/.npmrc
script:
    - npm run lint:js
    - npm run lint:css
    - npm run test
    # - commitlint-travis
