{
    "name": "@batfink/foobar",
    "main": "dist/Foobar.js",
    "module": "Foobar.jsx",
    "version": "1.0.2",
    "license": "MIT",
    "scripts": {
        "prepare": "yarn build",
        "clean": "rimraf dist && mkdir dist",
        "bundle": "rollup -c",
        "build": "npm-run-all clean bundle"
    },
    "dependencies": {
        "styled-components": "^4.0.3"
    },
    "peerDependencies": {
        "react": "^16.6.0",
        "react-dom": "^16.6.0"
    },
    "publishConfig": {
        "access": "public"
    }
}
