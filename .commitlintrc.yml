extends:
    - "@commitlint/config-conventional"
rules:
    type-empty:
        - 2
        - never
    # scope-empty:
        # - 2
        # - never
    scope-enum:
        - 2
        - always
        -
            - release
            - travis
            - eslint
            - stylelint
            - webpack
            - babel
            - commitlint
            - gitignore
            - jest
            - rollup
            - eslint
            - storybook
            - package.json
            - setup
            - readme
            - barbar
            - barbaz
            - bazar
